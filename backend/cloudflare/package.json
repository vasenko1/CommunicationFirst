{
  "name": "communication-first-signaling",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.5",
    "vitest": "^4.1.10",
    "wrangler": "^4.0.0"
  }
}
